# infer — Project Instructions

This project inherits the full development workflow and quality bar from the
global instructions at `~/.claude/CLAUDE.md`. Those rules apply here in full:
the development loop, execution rules, working principles, quality bar, and git
conventions. Read them as the baseline for every task in this repo.

## Project-specific notes

_None yet — add project-specific stack, commands, and conventions below as the
`infer` project takes shape (e.g. language/toolchain, how to run lint/types/tests,
the coverage floor once first measured, error taxonomy, and any deviations from
the global defaults)._
